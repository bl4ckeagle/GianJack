{% extends 'BaseTemplate1.html.twig ' %}


{% block title %}Photos{% endblock %}
{% block contentmaster %}
    <div class="photos">
        <div class="jumbotron">
            {{ dump() }}
        </div>
        <div class="jumbotron">


            {% for key, values in Values %}

                <p>{{ key }}{{ values }}</p>
            {% endfor %}




        </div>
    </div>

{% endblock %}
